import React from "react";
import classes from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={classes.root}>
      <h5>footer flavor text</h5>
    </footer>
  );
};

export default Footer;
